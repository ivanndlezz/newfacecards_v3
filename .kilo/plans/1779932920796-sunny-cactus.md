# Plan: Add Edit Overlay to Bio Buttons

## Objective
Add an edit overlay overlay to each button in the list that allows editing existing buttons. Each button must have a unique ID (already implemented via `Date.now()`) and the overlay should appear when entering edit mode.

## Changes Required

### 1. CSS Styles (in button-form-experience.html)
Add styling for `.edit-overlay` and `.edit-text` classes:
- Position overlay on top of each button (absolute/fixed positioning)
- Show on `data-status="edit"` state (similar to `.list-del-btn` pattern)
- Pencil/edit icon styling

### 2. HTML Template Update (line ~1889)
Modify `renderMainList()` to include edit overlay inside each `<li>`:
```html
<li>
  <div class="edit-overlay" data-action="edit-button" data-id="${b.id}">
    <span class="edit-text"></span>
  </div>
  <a href="${href}">...</a>
  <button class="list-del-btn" ...></button>
</li>
```

### 3. JavaScript Functions (in button-form-experience.html)
- Add `editButton(id)` function to populate the sheet with existing button data
- Add `ACTIONS.EDIT_BUTTON = "edit-button"` constant
- Handle edit action in the dispatcher (line 1984-2001)

### 4. State Management
- Add `editingButtonId` to state object `S` to track which button is being edited
- Modify `addButton()` to update existing button instead of adding new one when editing
- Add cancel edit functionality

## Implementation Order
1. Add CSS styles for edit overlay
2. Update `renderMainList()` HTML generation
3. Add `editButton()` function
4. Update action dispatcher
5. Modify `addButton()` to handle update vs create

## Notes
- Follow existing patterns (`.list-del-btn` for visibility on edit mode)
- Overlay should use same `data-action` delegation pattern
- Maintain consistency with existing grayscale/zinc color palette