// uiConfig.js – centralised UI constants
export const UI_ELEMS = Object.freeze({
  BTN_PERSONALIZAR: "btn-personalizar",
  MAIN_SETTINGS: "main-settings",
  CUSTOM_SETTINGS: "custom-settings",
  SHEET_HEADER_LEFT: "sheet-header-left",
  // Add other element IDs as needed
});

export const UI_STATUS = Object.freeze({
  HIDDEN: "hidden",
  ACTIVE: "active",
  CUSTOM: "custom",
  READY: "ready",
});

export const SELECTORS = Object.freeze({
  IMAGE_STYLE_CHECKED: 'input[name="image_style"]:checked',
  BREADCRUMB_BACK: ".breadcrumb-back",
  // Add other selectors as needed
});
export const UI_DEFAULTS = Object.freeze({
  IMAGE_STYLE: "classic",
});
